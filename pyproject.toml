[project]
name = "tensor-prism"
version = "1.6.5"
description = "Advanced model merging and enhancement nodes for ComfyUI with sophisticated techniques for blending, enhancing, and manipulating Stable Diffusion models"
license = { text = "GPL-3.0" }
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "torch>=1.12.0",
    "numpy>=1.21.0",
    "psutil>=5.8.0"
]
authors = [
    { name = "Arctenox" }
]
keywords = [
    "comfyui",
    "custom-nodes",
    "model-merging",
    "stable-diffusion",
    "sdxl",
    "ai",
    "machine-learning",
    "deep-learning",
    "tensor-operations",
    "spectral-merging"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Multimedia :: Graphics",
    "Operating System :: OS Independent",
    "Environment :: GPU :: NVIDIA CUDA",
    "Environment :: GPU :: AMD ROCm",
    "Environment :: GPU :: Apple Metal"
]

[project.urls]
Repository = "https://github.com/Arctenox/Tensor_Prism"
Issues = "https://github.com/Arctenox/Tensor_Prism/issues"
Discord = "https://discord.gg/UVXPdkgedh"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=22.0.0",
    "flake8>=4.0.0"
]

[build-system]
requires = ["setuptools>=45", "wheel", "setuptools-scm>=6.2"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["."]

[tool.setuptools.package-data]
"*" = ["*.py"]

[tool.comfy]
PublisherId = "arctenox"
DisplayName = "Tensor Prism"
Icon = "https://raw.githubusercontent.com/Arctenox/Tensor_Prism/main/icon.png"
